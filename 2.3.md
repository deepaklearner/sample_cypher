You're absolutely right — great catch! The `hireDate` future-date validation logic was unnecessarily **duplicated** in two places:

* Once **inside** the `check_new_rejections()` function.
* Again, **outside** in the Neo4j-empty fallback logic.

---

### ✅ Best Fix: Centralize `hireDate` check inside `check_new_rejections()`

To avoid duplication:

* Remove the **outer `hireDate` logic** from the fallback case.
* Let `check_new_rejections()` always handle it, even if Neo4j returns no labels.

---

### ✅ Updated Strategy

1. If Neo4j returns no labels, **still call `check_new_rejections()`** with just the early-filtered `employee_df`.
2. Inside `check_new_rejections()`, future `hireDate` violations will still be caught.

---

### ✅ Updated Full Code (cleaned and correct)

```python
import pandas as pd
from datetime import datetime
from neo4j import GraphDatabase

# === Neo4j Configuration ===
uri = "bolt://localhost:7687"
username = "neo4j"
password = "your_password"  # Replace with actual password
driver = GraphDatabase.driver(uri, auth=(username, password))

# === Neo4j Label Fetch ===
def fetch_user_labels(tx, emp_nums):
    query = """
    UNWIND $empNums AS empNum
    MATCH (u:User {employeeNumber: empNum})
    RETURN empNum AS employeeNumber, labels(u) AS labels
    """
    result = tx.run(query, empNums=emp_nums)
    return [record.data() for record in result]

# === Rejection Rule Handler ===
def check_new_rejections(df):
    reasons = {}

    # Rule 1: Contractor label present, but not Conversion
    if 'labels' in df.columns:
        has_contractor_label = df['labels'].apply(lambda labels: 'Contractor' in labels)
        is_not_conversion = df['recordType'] != 'Conversion'
        contractor_violations = df[has_contractor_label & is_not_conversion]
        for emp_num in contractor_violations['employeeNumber']:
            reasons.setdefault(emp_num, []).append("Contractor label present but recordType is not 'Conversion'")

        # Rule 2: A/L status but missing 'Active'/'OnLeave' label
        is_status_al = df['employmentStatus'].isin(['A', 'L'])
        missing_required_label = ~df['labels'].apply(lambda labels: any(l in ['Active', 'OnLeave'] for l in labels))
        status_violations = df[is_status_al & missing_required_label]
        for emp_num in status_violations['employeeNumber']:
            reasons.setdefault(emp_num, []).append("employmentStatus in ['A','L'] but missing 'Active'/'OnLeave' label in Neo4j")

    # Rule 3: hireDate in the future
    if 'hireDate' in df.columns:
        df['hireDate'] = pd.to_datetime(df['hireDate'], errors='coerce')
        future_mask = df['hireDate'] > pd.Timestamp.today().normalize()
        future_violations = df[future_mask]
        for emp_num in future_violations['employeeNumber']:
            reasons.setdefault(emp_num, []).append("Hire date is in the future")

    # Combine all into a DataFrame
    mismatch_df = pd.DataFrame([
        {'employeeNumber': emp_num, 'Reason': '; '.join(reasons_list)}
        for emp_num, reasons_list in reasons.items()
    ])
    return mismatch_df

# === Main Processing ===

# Step 1: Filter userType = 'Employee'
employee_df = df[df['userType'] == 'Employee'].copy()

# Step 2: Early filter — reject Conversion + invalid status
invalid_conversion_df = employee_df[
    (employee_df['recordType'] == 'Conversion') &
    (~employee_df['employmentStatus'].isin(['A', 'L']))
].copy()
invalid_conversion_df['Reason'] = "Conversion record with employmentStatus not in ['A', 'L']"

# Remove early rejections from further processing
employee_df = employee_df.drop(index=invalid_conversion_df.index)

# Step 3: Get Neo4j labels for remaining employees
employee_numbers = employee_df['employeeNumber'].tolist()
with driver.session() as session:
    labels_data = session.read_transaction(fetch_user_labels, employee_numbers)

labels_df = pd.DataFrame(labels_data)

# Step 4: If Neo4j returned nothing, skip label-based checks
if labels_df.empty or 'employeeNumber' not in labels_df.columns:
    print("⚠️ No matching User nodes found in Neo4j.")
    # Still apply future hire date checks and return all issues
    label_free_check_df = pd.concat([employee_df, invalid_conversion_df], ignore_index=True)
    mismatch_df = check_new_rejections(label_free_check_df)
    valid_df = df[~df['employeeNumber'].isin(mismatch_df['employeeNumber'])]
    mismatch_df.to_csv("mismatch_log.csv", index=False)
    print("✅ Mismatch log saved (no Neo4j users)")
    print(f"✅ Valid records: {len(valid_df)}")
    print(f"❌ Mismatched records: {len(mismatch_df)}")
    exit()

# Step 5: Merge label data
merged_df = employee_df.merge(labels_df, on='employeeNumber', how='inner')

# Step 6: Apply rejection logic
mismatch_df = check_new_rejections(merged_df)

# Step 7: Add early rejected Conversion rows
if not invalid_conversion_df.empty:
    mismatch_df = pd.concat([mismatch_df, invalid_conversion_df[['employeeNumber', 'Reason']]], ignore_index=True)

# Step 8: Filter valid records from original df
valid_df = df[~df['employeeNumber'].isin(mismatch_df['employeeNumber'])]

# Step 9: Write mismatches to CSV
mismatch_df.to_csv("mismatch_log.csv", index=False)

print("✅ Mismatch log saved: mismatch_log.csv")
print(f"✅ Valid records count: {len(valid_df)}")
print(f"❌ Mismatched records count: {len(mismatch_df)}")
```

---

### ✅ Summary of Fix:

* `hireDate` rule is **only** inside `check_new_rejections()` now.
* Eliminated duplicated logic.
* More maintainable, cleaner flow.

Let me know if you'd like this packaged as a utility module or with unit tests.
