Below is the clearest way to think about your question — **YES, your current rejection logic must change** — and I'll explain *why*, *how*, and *what the new flow must look like* given the new PAS requirement.

---

# ✅ **Short Answer**

**Yes — you must change the rejection logic.**
Your older logic:

> “Reject if neo4j.PrimaryAuth is TRUE or neo4j.accountType != 'Primary' "

is **no longer valid**, because the new requirement **explicitly allows PrimaryAuth to be reassigned / switched**, and allows Primary ↔ Secondary switching based on new PAS rules.

So the ETL must no longer skip those users — instead it must *reprocess* them.

---

# 🧠 **Detailed Reasoning**

## ## Why your old rejection logic existed:

You originally rejected EDW rows when:

* User already has **PrimaryAuth = True** in neo4j
  → meaning PAS is already determined
* User’s accountType in neo4j != “Primary”
  → meaning EDW was not supposed to override a non-primary account

This ensures:

* **PAS computed once (first time only)**
* **Subsequent EDW imports do not disrupt existing Neo4j structure**

---

# ❗ But Requirement Has Changed

The new requirement says:

### **You must compute PAS again using EDW data**

and:

### **If PAS changes → retrieve all accounts for user from Neo4j and update them**

This means:

* PAS is no longer “write once”
* PAS can be recalculated
* PAS can be reassigned from one account to another
* Potentially **multiple domains** can be rearranged Primary/Secondary based on PAS

Therefore:

## ❌ You cannot reject users based on existing PrimaryAuth in Neo4j anymore

Because Neo4j’s PAS state is no longer the “source of truth” —
EDW-derived PAS rules override it.

---

# ✔ So what should the new logic be?

You must replace the old rejection logic with a **"PAS change detection" pipeline**.

Below is a clean rewrite of your required flow.

---

# ✅ **New Required Flow (Fully Clarified)**

### **STEP 1 — Read all accounts from EDW**

For each employee:

* List all accounts
* Compute candidate PAS using EDW logic:

  ```
  If domain in ['CORP','CSARMARK','CVS']
  AND "cloud" in extensionattribute3
  AND user has no other primaryAuth account
  THEN mark this one as PrimaryAuth
  ```

---

### **STEP 2 — Compare EDW PAS vs Neo4j PAS**

* If **Neo4j has no PrimaryAuth**, assign EDW PAS.
* If **EDW PAS == Neo4j PAS** → nothing to do.
* If **EDW PAS != Neo4j PAS** → PAS changed → go to Step 3.

> ✔ This is the replacement for the old rejection logic.

---

### **STEP 3 — If PAS changed → Retrieve all Neo4j accounts for user**

You must rebuild the user’s Primary/Secondary structure.

---

### **STEP 4 — Check if the user has Secondary accounts**

**Case A:** User has NO secondary accounts →
→ simply flip the new EDW PAS account to PrimaryAuth = true

**Case B:** User HAS secondary accounts
→ evaluate switching accounts

---

### **STEP 5 — If a secondary account has PrimaryAuth set**

Meaning Neo4j contains:

* An account marked PrimaryAuth=true
* But EDW PAS says a *different* account should be PrimaryAuth

Then:

* **Switch the accounts**

  * The PAS account becomes the Primary
  * Old PrimaryAuth becomes Secondary
  * Apply domain consistency

---

### **STEP 6 — Check if any other domains have this PAS account as secondary**

This means:

* User may have *multiple accounts across various domains*
* PAS account may appear as secondary in a domain cluster

Then:

* **Switch all domain clusters** so PrimaryAuth is aligned consistently

---

# 🧠 **What this means conceptually for IAM systems**

This requirement is essentially:

### **PAS is no longer “write-once”; it is now mutable.**

#### The IAM best practice pattern that matches your scenario is:

→ **Authoritative source = EDW derived rules**
→ **Neo4j reflects computed PAS state**
→ **If rules change, Neo4j must be rebalanced**

This makes sense — many IAM systems (SailPoint, Saviynt, IAM/IGA platforms) follow this pattern:

* PAS is derived
* If new data arrives, PAS must be recomputed
* Primary/Secondary roles may change
* All accounts and relationships must be rediscovered and reconciled

This is exactly what your requirements imply.

---

# ⭐ **Conclusion**

### ✔ YES — You must remove / rewrite the old rejection logic:

**Old Logic (to delete):**

> “Reject if neo4j.PrimaryAuth = True OR accountType != 'Primary' ”

**New Logic (required):**

> “Process EVERY user.
> Detect if EDW PAS ≠ Neo4j PAS.
> If changed, retrieve all Neo4j accounts and rebalance PAS + primary/secondary roles.”

---

# If you want, I can also help you by:

✅ rewriting your delta detection logic
✅ rewriting your PAS recomputation flow
✅ rewriting your Primary/Secondary switching algorithm
✅ redesigning the overall ETL architecture
✅ producing a cleaner pseudo-code version

Just tell me what you’d like next.
