Perfect! Let’s visualize the updated **PrimaryAuth reconciliation logic** as a **flowchart**. I’ll describe it in a way you can directly translate to an actual diagram in Lucidchart, Draw.io, or even in your documentation.

---

# **Flowchart: EDW → Neo4j PrimaryAuth Reconciliation**

```
START
   │
   ▼
Fetch EDW accounts for user
   │
   ▼
Compute "eligible primary accounts" from EDW
(TargetSystem in ['CORP','CSARMARK','CVS'] AND 'cloud' in extensionAttribute3)
   │
   ▼
Are there multiple eligible accounts?
   ├─ Yes ─► Optional: Apply business priority OR pick first as primary
   │           (Log warning about ambiguity)
   │
   └─ No ─► Continue
   │
   ▼
Is there a single eligible account?
   ├─ Yes ─► Mark as computedPrimary
   │
   └─ No ─► computedPrimary = None
   │
   ▼
Fetch Neo4j accounts for user
   │
   ▼
Is there an existing Neo4j primary?
   ├─ Yes ─►
   │        Does Neo4j primary match computedPrimary?
   │           ├─ Yes ─► No change needed
   │           └─ No ─► Demote old primary to secondary
   │                    Assign computedPrimary as primary
   └─ No ─►
            Assign computedPrimary as primary (if exists)
   │
   ▼
Assign secondary accounts
(Every other account not marked as primary becomes Secondary)
   │
   ▼
Propagate PAS primary across other domains
   ├─ Any domain has PAS primary incorrectly marked as secondary?
   │      └─ Yes ─► Switch accounts: make PAS account primary in domain
   │
   ▼
Update domain-related fields in ETL table:
  - domains
  - primaryAuthDomain
  - primaryAuthDomainName
  - corpAccountNames
   │
   ▼
END / Proceed to Step 7 (Compute oneIDs)
```

---

# **Notes on Flow**

1. **EDW rules are authoritative** — Neo4j is updated, not used for rejection.
2. **Multiple eligible accounts**:

   * If strict business rule exists → reject
   * Otherwise → pick first or apply priority
3. **Secondary accounts** are automatically derived.
4. **PAS system** (CORP/CSARMARK/CVS + cloud) is the authoritative source for cross-domain primary assignment.

---

If you want, I can **also create a graphical version** of this flowchart for you, ready to paste into presentations or documentation, showing boxes, decision diamonds, and arrows — like a professional visual diagram.

Do you want me to do that next?
