Below is a **clean, architect-level interpretation** of your updated requirement — including how to **compute PrimaryAuth**, when to **switch accounts**, and where your **old rejection logic must be replaced**.

I will break it down so you end up with a **full, implementable algorithm**.

---

# 🚨 First: What the New Requirement Is REALLY Saying

Your new steps describe a **domain-wide Primary/Secondary reconciliation process** driven by **EDW-derived PrimaryAuth**, NOT by Neo4j.

The meaning behind each step is:

1. **EDW → Compute which account SHOULD be PrimaryAuth** (based on your rule)
2. **Compare with Neo4j**

   * If mismatch → reconcile
3. **Make sure each user has exactly 1 primary and then secondary accounts**
4. **If the secondary account happens to be marked primary (incorrectly), fix it**
5. **Propagate PAS primary to all domains** — i.e., if the PrimaryAuth in PAS changes, align all domain accounts with that primary/secondary structure.

This is classic **IAM multi-domain account reconciliation**.

---

# 🧠 STEP-BY-STEP INTERPRETATION

I’ll express your requirement in clear, logical form.

---

# **STEP 1 — Read EDW accounts and compute the “intended” PrimaryAuth**

You said:

* PrimaryAuth is **not in EDW**
* It is computed using:

```
Eligible Primary = 
    targetSystem IN ['CORP','CSARMARK','CVS'] 
    AND extensionAttribute3 contains 'cloud'
```

From the EDW batch:

### Compute:

```
eligibleAccounts = all EDW accounts for the user that meet eligibility criteria
```

Then:

* If `eligibleAccounts` = 1 → **that account SHOULD be primary**
* If `eligibleAccounts` > 1 → **reject (ambiguous)**
* If `eligibleAccounts` = 0 → **no primary should exist**

This gives you:

```
computedPrimary = the single eligible account (or null)
```

---

# **STEP 2 — Compare computed EDW PrimaryAuth vs Neo4j PrimaryAuth**

Retrieve from Neo4j:

```
neo4jPrimary = the account currently marked PrimaryAuth=true
```

Comparison:

| computedPrimary | neo4jPrimary      | Meaning                                         |
| --------------- | ----------------- | ----------------------------------------------- |
| null            | null              | OK, no primary                                  |
| non-null        | same account      | OK                                              |
| non-null        | null              | Assign primary                                  |
| non-null        | different account | **PrimaryAuth changed → reconciliation needed** |
| null            | non-null          | **demote/remove PrimaryAuth**                   |

If PrimaryAuth changed → **retrieve all user accounts from Neo4j** (your requirement step 2).

---

# **STEP 3 — If user has no secondary account → create/update secondary**

After reconciling primary:

Check in Neo4j:

```
hasSecondary = any account for user where accountType = 'Secondary'
```

If `hasSecondary = false`:

→ you must **assign secondary account(s)** based on domain rules
(usually same targetSystem domain; or all accounts except the primary).

This satisfies your requirement step 3.

---

# **STEP 4 — If any secondary account is incorrectly marked PrimaryAuth → switch**

Your requirement:

> "check if PrimaryAuth set for secondary account → If yes switch accounts"

This means:

* If Neo4j accidentally has a Secondary account set as PrimaryAuth=true
* And the computed EDW primary is DIFFERENT

→ Swap:

```
newPrimary = computedPrimary (from EDW rules)
newSecondary = the old primary
```

Mark them:

```
newPrimary.PrimaryAuth = true
newSecondary.PrimaryAuth = false
```

This ensures domain correctness.

---

# **STEP 5 — Propagate PrimaryAuth to all domains**

Your requirement:

> "If any domain has this PAS account as secondary → switch accounts to primary and secondary in each domain to match PAS."

This means:

* PAS (your primary eligible system: CORP/CSARMARK/CVS + cloud) **is the authoritative primary source**
* All other domains (AD, LDAP, CX, etc.) must follow its primary/secondary assignment

So:

### For each domain:

```
IF the PAS primary is stored as secondary in that domain:
    switch → make it primary
    demote the current domain primary to secondary
```

This ensures **cross-domain consistency**.

---

# ✔️ Final Complete Algorithm (Clear, Implementable)

### **INPUT:** EDW accounts for the user

### **OUTPUT:** Correct Neo4j primary/secondary assignments

---

## **Phase 1 — EDW Computation**

1. Identify all eligible primary accounts
2. If >1 → reject
3. Determine computedPrimary (null or 1 account)

---

## **Phase 2 — Neo4j Comparison**

Fetch Neo4j accounts:

4. Identify neo4jPrimary
5. If computedPrimary ≠ neo4jPrimary:
   → Reconciliation triggered
   → Load all Neo4j accounts

---

## **Phase 3 — Reconcile PrimaryAuth**

6. If computedPrimary = null → demote any existing primary in Neo4j
7. If computedPrimary ≠ null:

   * Set computedPrimary.PrimaryAuth = true
   * Others = false

---

## **Phase 4 — Secondary Account Check**

8. If user has no secondary account → assign secondary(s)
   (any account not primary becomes secondary)

---

## **Phase 5 — Domain Consistency**

9. For each domain:

   * If PAS primary is marked secondary → swap
   * Ensure domain primary matches overall primary
   * Ensure remaining accounts become secondary

---

# 🚨 So what happens to your OLD REJECTION LOGIC?

Your old logic:

```
Reject if neo4j has PrimaryAuth = true 
or neo4j accountType != 'Primary'
```

### ❌ This logic is no longer valid.

Because:

* Neo4j is **not authority anymore**
* EDW rule computation + cross-domain reconciliation **replaces** simple validation
* You now **fix mismatches**, not reject them

So the rejection rule must be removed and replaced by **reconciliation logic**.

---

# 🎯 Summary in Plain English

You must:

* Compute PrimaryAuth from EDW using rules
* Compare with Neo4j
* If mismatch → FIX, not reject
* Ensure domain alignment
* Ensure only 1 PrimaryAuth per user
* Ensure secondary accounts exist
* Ensure all domains follow PAS system primary

---

If you want, I can give you:

👉 Full pseudocode
👉 A flowchart
👉 A decision table
👉 SQL or Python implementation examples

Just tell me what format you need.
