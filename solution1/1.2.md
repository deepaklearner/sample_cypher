Based on your new PAS requirement, **these are the functions that MUST be modified**:

---

# ✅ **Functions You Need to Modify**

### **1. `delta_src_vs_gdb_usrs_usraccts_profiles`**

Because it currently **rejects** EDW accounts when:

* `PrimaryAuth == True` in Neo4j
* or `accountType != 'Primary'` in Neo4j

This logic must change to support PAS re-evaluation.

---

### **2. `compute_oneIDs_for_useraccounts`**

Because after PAS changes are detected, you must rebuild:

* primary accounts
* secondary accounts
* reassignment logic

This requires modifying how remaining accounts are split and reassigned.

---

### **3. `determine_primary_accounts`**

Because the new rule says:

* recompute PrimaryAuth from EDW
* switch Primary/Secondary if PAS changed
* update domains consistently

This function directly contains the "determine primary" logic = must change.

---

### **4. `compute_PrimaryAuthsystem_nodes`**

This function implements the old rules for determining PrimaryAuth.
But now PAS may:

* change
* be reassigned
* require domain-level switching

So this function must be updated to reflect the new PAS rules.

---

### **5. `determine_secondary_accounts_list`**

Because switching primary/secondary accounts due to PAS changes affects secondary accounts too.

---

### **6. `determine_privileged_accounts`**

*Possibly*, depending on your design —
If privileged accounts must react to PAS reassignment (e.g., they can't become primary), this logic may need updates.

---

# 📌 **Minimum Required Changes**

If you want *absolutely minimal* changes:

### **MUST modify**:

* `delta_src_vs_gdb_usrs_usraccts_profiles`
* `compute_PrimaryAuthsystem_nodes`
* `determine_primary_accounts`
* `compute_oneIDs_for_useraccounts`

These four are where PAS/Primary switching happens.

---

# If you want, I can highlight the exact code sections inside each function that need modification, line-by-line.
